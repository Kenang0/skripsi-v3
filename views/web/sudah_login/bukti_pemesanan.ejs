<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Ngiklan Murah</title>


    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">

    <link href="https://cdn.jsdelivr.net/npm/fullcalendar@6.1.11/index.global.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <style>
        .navbar {
            transition: background-color 0.3s;
            background-color: transparent;
        }

        .navbar.scrolled {
            background-color: rgb(255, 255, 255);
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        }


        .navbar a {
            transition: all 0.3s ease;
            text-decoration: none;
        }

        /* Hover untuk nav menu dan ikon */
        .navbar a:hover {
            color: #0d6efd !important;
            transform: scale(1.05);
            opacity: 0.9;
        }


        .navbar i {
            transition: transform 0.3s ease;
        }

        .navbar i:hover {
            transform: scale(1.2);
        }

        /* Hover untuk tombol */
        .navbar .btn:hover {
            transform: scale(1.05);
            opacity: 0.9;
            box-shadow: 0 0 8px rgba(0, 0, 0, 0.1);
        }

        .kategori-item {
            display: flex;
            flex-direction: column;
            align-items: center;
            transition: transform 0.3s ease;
            cursor: pointer;
        }

        .kategori-item:hover {
            transform: scale(1.05);
        }

        .kategori-icon {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            overflow: hidden;

            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
        }

        .kategori-icon img {
            width: 60%;
            height: auto;
        }

        /* Scrollbar styling opsional */
        .scroll-horizontal::-webkit-scrollbar {
            height: 4px;
        }

        .scroll-horizontal::-webkit-scrollbar-thumb {
            background-color: rgba(0, 0, 0, 0.2);
            border-radius: 10px;
        }

        .container-detail {
            max-width: 800px;
            margin: auto;
            border: 1px solid #ccc;
            font-family: Arial, sans-serif;
            font-size: 13px;
            color: #000;
            margin: auto;
            padding: 70px;
            background: #fff;
        }

        .logo {
            width: 140px;
        }

        .flex-space {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
        }

        h1 {
            font-size: 20px;
            font-weight: bold;
        }

        .section {
            margin-top: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 15px;
        }

        th,
        td {
            border: 1px solid #ccc;
            padding: 8px;
            vertical-align: top;
        }

        th {
            background: #f5f5f5;
        }

        .text-right {
            text-align: right;
        }

        .text-bold {
            font-weight: bold;
        }

        .green {
            color: #03AC0E;
        }

        .gray {
            color: #666;
            font-size: 12px;
        }

        .watermark {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            opacity: 0.1;
            font-size: 72px;
            color: #999;
            pointer-events: none;
            z-index: 0;
        }

        .footer {
            margin-top: 30px;
            font-size: 12px;
        }

        .placeholder-img {
            width: 100px;
            margin-top: 15px;
        }
    </style>
</head>

<body>

    <!-- navbar -->
    <nav class="navbar navbar-expand-lg fixed-top">
        <div class="container-fluid px-4">
            <!-- Logo -->
            <img src="/foto/logo_ngiklanmurah.png" alt="Logo" style="height: 20px; padding-right: 5px;">
            <a href="/home-page" class="navbar-brand fs-5">NGIKLAN MURAH</a>

            <div class="d-flex align-items-center ms-auto">
                <!-- Toggler -->
                <button class="navbar-toggler me-2" type="button" data-bs-toggle="collapse"
                    data-bs-target="#navbarCollapse">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <!-- Profil User Khusus Mobile -->
                <% if (pemesanan) { %>
                    <div class="d-block d-lg-none d-flex align-items-center">
                        <img src="/uploads/profile_pic/<%= pemesanan.photo_user %>" alt="User" class="rounded-circle"
                            style="width: 32px; height: 32px; object-fit: cover;">
                    </div>
                    <% } %>
            </div>

            <!-- Isi Navbar -->
            <div class="collapse navbar-collapse" id="navbarCollapse">
                <!-- Menu Tengah -->
                <ul class="navbar-nav mx-auto mb-3 mb-lg-0 text-center">
                    <li class="nav-item"><a class="nav-link text-dark" href="/home-page">Home</a></li>
                    <li class="nav-item"><a class="nav-link text-dark" href="#">Kategori</a></li>
                    <li class="nav-item"><a class="nav-link text-dark" href="#">Tentang Kami</a></li>
                </ul>

                <!-- Ikon dan Button -->
                <div class="d-flex flex-column flex-lg-row align-items-center gap-3 text-center">
                    <!-- ICON -->
                    <div class="d-flex flex-row flex-wrap justify-content-center gap-3">
                        <div class="d-flex flex-column align-items-center">
                            <a href="#" class="text-dark fs-6"><i class="bi bi-cart3"></i></a>
                            <small>Keranjang</small>
                        </div>
                        <div class="d-flex flex-column align-items-center">
                            <a href="#" class="text-dark fs-6"><i class="bi bi-opencollective"></i></a>
                            <small>Riwayat</small>
                        </div>
                        <div class="d-flex flex-column align-items-center">
                            <a href="/dalam-progress" class="text-dark fs-6"><i
                                    class="bi bi-clipboard-check-fill"></i></a>
                            <small>Progress</small>
                        </div>
                    </div>

                    <!-- Button -->
                    <!-- Jika user sudah login -->
                    <div class="user-profile d-none d-lg-block ms-3">
                        <a href="/pengaturan-akun">
                            <img src="/uploads/profile_pic/<%= pemesanan.photo_user %>" alt="User"
                                class="rounded-circle" style="width: 32px; height: 32px; object-fit: cover;"></a>
                    </div>

                </div>
            </div>
        </div>
    </nav>

    <section style="padding-top: 100px;">
        <div class="container-detail">
            <div class="flex-space">
                <img src="/foto/logo_ngiklanmurah.png" style="width: 90px; height: 70px;" class="logo"
                    alt="Ngiklanmurah Logo">
                <div style="text-align:right; padding-right: 20px;">
                    <h1>Bukti Pemesanan</h1>
                    <p><strong>NOMOR PEMESANAN : <%= pemesanan.id_pemesanan %></strong></p>
                </div>
            </div>

            <div class="section flex-space">
                <div>
                    DITERBITKAN ATAS <br />
                    Nama Toko :<strong>
                        <%= pemesanan.nama_toko_vendor %>
                    </strong>
                </div>
                <div>
                    <% const options={ day: 'numeric' , month: 'long' , year: 'numeric' }; %>
                        <strong>UNTUK</strong><br />
                        Pembeli : <strong>
                            <%= pemesanan.full_name %>
                        </strong><br />
                        Tanggal Pemesanan : <%= new Date(pemesanan.tanggal_pemesanan).toLocaleDateString("id-ID",
                            options) %><br />
                            Nomor Tlp : <%= pemesanan.nomor_tlp %><br />
                                Email : <%= pemesanan.email %>
                </div>
            </div>

            <hr style="margin: 10px 0;" />

            <div class="section">
                <table>
                    <thead>
                        <tr>
                            <th>INFO PRODUK</th>
                            <th>Jenis</th>
                            <th>JUMLAH</th>
                            <th>HARGA SATUAN</th>
                            <th>TOTAL HARGA</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td>
                                <img src="/uploads/produk_img/<%= pemesanan.photo_produk %>"
                                    class="placeholder-img"><br />
                                <span class="text-bold">
                                    <%= pemesanan.nama_produk %>
                                </span>
                            </td>
                            <td>
                                <%= pemesanan.tipe_kategori %> <br>
                                    <%= pemesanan.jenis_target%>
                            </td>
                            <td>
                                <%= pemesanan.jumlah_pemesanan %>
                            </td>
                            <td>Rp<%= pemesanan.harga.toLocaleString("id-ID") %>
                            </td>
                            <td>Rp<%= (pemesanan.harga * pemesanan.jumlah_pemesanan).toLocaleString("id-ID") %>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>

            <!-- Detail SMS -->
            <% if (detailSMS) { %>
                <div class="section">
                    <table>
                        <tbody>
                            <tr>
                                <td colspan="2" class="text-bold" style="background:#f5f5f5; text-align: center;">DETAIL
                                    PEMESANAN</td>
                            </tr>
                            <tr>
                                <td>Nomor Penerima Bukti Tayang</td>
                                <td>
                                    <%= detailSMS.nomor_penerima_bukti_tayang %>
                                </td>
                            </tr>
                            <tr>
                                <td>Masking</td>
                                <td>
                                    <%= detailSMS.masking %>
                                </td>
                            </tr>
                            <tr>
                                <td>Target Device Penerima</td>
                                <td>
                                    <%= detailSMS.tipe_device_penerima %>
                                </td>
                            </tr>
                            <tr>
                                <td>Target Umur Penerima</td>
                                <td>
                                    <%= detailSMS.target_umur %>
                                </td>
                            </tr>
                            <tr>
                                <td>Tanggal Pengiriman</td>
                                <td>
                                    <% const tglStart=new Date(detailSMS.tanggal_pengiriman_start); const tglEnd=new
                                        Date(detailSMS.tanggal_pengiriman_end); const
                                        sameDate=tglStart.toDateString()===tglEnd.toDateString(); const options={
                                        day: 'numeric' , month: 'long' , year: 'numeric' }; %>
                                        <% if (sameDate) { %>
                                            <%= tglStart.toLocaleDateString("id-ID", options) %>
                                                <% } else { %>
                                                    <%= tglStart.toLocaleDateString("id-ID", options) %> s/d <%=
                                                            tglEnd.toLocaleDateString("id-ID", options) %>
                                                            <% } %>
                                </td>
                            </tr>
                            <tr>
                                <td>Jam Pengiriman</td>
                                <td>
                                    <%= detailSMS.jam_pengiriman %>
                                </td>
                            </tr>

                            <% if (!(pemesanan.jenis_target?.toLowerCase().includes('lba'))) { %>
                                <tr>
                                    <td>File Nomor</td>
                                    <td>
                                        <% if (detailSMS.file_nomor_sms_pdf) { %>
                                            <a href="/uploads/sms_files/<%= detailSMS.file_nomor_sms_pdf %>"
                                                class="btn btn-sm btn-primary" download>
                                                Download File Nomor CSV
                                            </a>
                                            <% } else { %>
                                                <em>Menggunakan file nomor dari vendor</em>
                                                <% } %>
                                    </td>
                                </tr>
                                <% } %>



                                    <% if (pemesanan.jenis_target?.toLowerCase().includes('lba')) { %>
                                        <tr>
                                            <td>Latitude dan Longitude</td>
                                            <td>
                                                <%= detailSMS.dps_latitude %>
                                                    <%= detailSMS.dps_longitude %>
                                            </td>
                                        </tr>
                                        <tr>
                                            <td>Alamat Pengiriman</td>
                                            <td>
                                                <%= detailSMS.alamat_target_sms %>
                                            </td>
                                        </tr>
                                        <% } %>

                                            <tr class="text-bold">
                                                <td>Teks Iklan</td>
                                                <td>
                                                    <%= detailSMS.teks_iklan_sms %>
                                                </td>
                                            </tr>
                        </tbody>
                    </table>
                </div>

                <!-- SMS jika LBA -->
                <% if (pemesanan.jenis_target?.toLowerCase().includes('lba') ||
                    pemesanan.jenis_target?.toLowerCase().includes('mms')) { %>
                    <div class="section">
                        <div
                            style="display: flex; gap: 20px; margin-top: 10px; align-items: flex-start; flex-wrap: wrap;">

                            <% if (pemesanan.jenis_target?.toLowerCase().includes('lba')) { %>
                                <div style="max-width: 500px; width: 100%;">
                                    <h4 style="margin-bottom: 8px;">Peta Lokasi Target</h4>
                                    <div id="map" data-lat="<%= detailSMS.dps_latitude %>"
                                        data-lng="<%= detailSMS.dps_longitude %>"
                                        style="height: 300px; border: 1px solid #ccc;">
                                    </div>
                                </div>
                                <% } %>
                                    <!-- SMS jika MMS -->
                                    <% if (pemesanan.jenis_target?.toLowerCase().includes('mms')) { %>
                                        <div style="max-width: 300px; flex-shrink: 0;">
                                            <h4 style="margin-bottom: 8px;">Foto yang Digunakan</h4>
                                            <% if (detailSMS.file_foto_mms) { %>
                                                <img src="/uploads/sms_files/<%= detailSMS.file_foto_mms %>"
                                                    alt="Foto MMS" style="width: 100%; border: 1px solid #ccc;" />
                                                <% } else { %>
                                                    <em>Tidak ada foto MMS yang diunggah.</em>
                                                    <% } %>
                                        </div>
                                        <% } %>

                        </div>
                    </div>
                    <!-- Detail SMS END -->

                    <!-- script buat Google Maps -->
                    <script>
                        function initMap() {
                            const mapDiv = document.getElementById("map");
                            const lat = parseFloat(mapDiv.dataset.lat);
                            const lng = parseFloat(mapDiv.dataset.lng);

                            const lokasi = { lat, lng };
                            const map = new google.maps.Map(mapDiv, {
                                zoom: 17,
                                center: lokasi,
                                mapTypeId: 'roadmap',
                                fullscreenControl: false,
                                mapTypeControl: false,
                                streetViewControl: false,
                                zoomControl: true
                            });

                            new google.maps.Marker({
                                position: lokasi,
                                map: map
                            });
                        }
                    </script>

                    <script async defer
                        src="https://maps.googleapis.com/maps/api/js?key=<%= googleMapsApiKey %>&callback=initMap">
                        </script>
                    <% } %>
                        <% } %>
                            <script src="https://cdn.jsdelivr.net/npm/fullcalendar@6.1.11/index.global.min.js"></script>

                            <!--Detail Radio  -->
                            <% if (detailRadio && detailRadio.length> 0) { %>
                                <div class="section">
                                    <h4 class="mb-3 text-center">Jadwal Penayangan Radio</h4>
                                    <div id="calendarRadio" style="max-width: 800px; margin: auto;"></div>
                                </div>
                                <!-- FullCalendar -->
                                <script
                                    src="https://cdn.jsdelivr.net/npm/fullcalendar@6.1.11/index.global.min.js"></script>
                                <script>
                                    document.addEventListener('DOMContentLoaded', function () {
                                        const calendarEl = document.getElementById('calendarRadio');
                                        if (calendarEl) {
                                            const calendar = new FullCalendar.Calendar(calendarEl, {
                                                initialView: 'dayGridMonth',
                                                events: <%- JSON.stringify(eventRadio || []) %>,
                                                eventColor: '#03AC0E',
                                                eventTimeFormat: {
                                                    hour: '2-digit',
                                                    minute: '2-digit',
                                                    hour12: false // ⏱️ format 24 jam
                                                },
                                                dayCellDidMount: function (arg) {
                                                    const hariMerah = <%- JSON.stringify(hariTayangRadio || []) %>;
                                                    if (hariMerah.includes(arg.date.getDay())) {
                                                        arg.el.style.backgroundColor = '#ffdddd';
                                                    }
                                                }
                                            });
                                            calendar.render();
                                        }
                                    });
                                </script>

                                <script>
                                    console.log("📦 eventRadio dari server:", <%- JSON.stringify(eventRadio || []) %>);
                                </script>
                                <% } %>
                                    <div class="footer gray">
                                        Invoice ini sah dan diproses oleh komputer.<br />
                                        Silakan email kami apabila kamu membutuhkan bantuan.<br />
                                        <a href="#">placeholder@email.com</a>
                                    </div>

                                    <div class="watermark">LUNAS</div>
        </div>
    </section>

    <!-- footer -->
    <footer class="bg-dark text-white mt-5 pt-4 pb-3">
        <div class="container">
            <div class="row text-center text-md-start">
                <!-- Brand dan Deskripsi -->
                <div class="col-md-4 mb-4">
                    <h5 class="fw-bold">NGIKLAN MURAH</h5>
                    <p class="small">Solusi mudah dan terjangkau untuk semua kebutuhan iklan Anda. Billboard, Radio, SMS
                        dan
                        lainnya.</p>
                </div>

                <!-- Navigasi -->
                <div class="col-md-4 mb-4">
                    <h6 class="fw-bold">Navigasi</h6>
                    <ul class="list-unstyled">
                        <li><a href="#" class="text-white text-decoration-none small">Home</a></li>
                        <li><a href="#" class="text-white text-decoration-none small">Fitur</a></li>
                        <li><a href="#" class="text-white text-decoration-none small">Harga</a></li>
                    </ul>
                </div>

                <!-- Kontak -->
                <div class="col-md-4 mb-4">
                    <h6 class="fw-bold">Kontak</h6>
                    <p class="small mb-1"><i class="bi bi-envelope me-2"></i>support@ngiklanmurah.com</p>
                    <p class="small"><i class="bi bi-telephone me-2"></i>+62 812-3456-7890</p>
                </div>
            </div>

            <!-- Copyright -->
            <div class="text-center border-top pt-3 small">
                © 2025 NgiklanMurah. All rights reserved.
            </div>
        </div>
    </footer>

    <script>
        // Efek scroll navbar
        window.addEventListener('scroll', function () {
            const navbar = document.querySelector('.navbar');
            navbar.classList.toggle('scrolled', window.scrollY > 50);
        });
    </script>



</body>

</html>
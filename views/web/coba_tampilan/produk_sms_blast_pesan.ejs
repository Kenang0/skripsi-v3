<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Ngiklan Murah</title>


    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <style>
        .navbar {
            transition: background-color 0.3s;
            background-color: transparent;
        }

        .navbar.scrolled {
            background-color: rgb(255, 255, 255);
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        }


        .navbar a {
            transition: all 0.3s ease;
            text-decoration: none;
        }

        /* Hover untuk nav menu dan ikon */
        .navbar a:hover {
            color: #0d6efd !important;
            transform: scale(1.05);
            opacity: 0.9;
        }


        .navbar i {
            transition: transform 0.3s ease;
        }

        .navbar i:hover {
            transform: scale(1.2);
        }

        /* Hover untuk tombol */
        .navbar .btn:hover {
            transform: scale(1.05);
            opacity: 0.9;
            box-shadow: 0 0 8px rgba(0, 0, 0, 0.1);
        }

        .kategori-item {
            display: flex;
            flex-direction: column;
            align-items: center;
            transition: transform 0.3s ease;
            cursor: pointer;
        }

        .kategori-item:hover {
            transform: scale(1.05);
        }

        .kategori-icon {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            overflow: hidden;

            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
        }

        .kategori-icon img {
            width: 60%;
            height: auto;
        }

        /* Scrollbar styling opsional */
        .scroll-horizontal::-webkit-scrollbar {
            height: 4px;
        }

        .scroll-horizontal::-webkit-scrollbar-thumb {
            background-color: rgba(0, 0, 0, 0.2);
            border-radius: 10px;
        }
    </style>
</head>

<body>

    <!-- navbar -->
    <nav class="navbar navbar-expand-lg fixed-top">
        <div class="container-fluid px-4">
            <!-- Logo -->
            <img src="/foto/logo_ngiklanmurah.png" alt="Logo" style="height: 20px; padding-right: 5px;">
            <a href="#" class="navbar-brand fs-5">NGIKLAN MURAH</a>

            <!-- Toggler -->
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
                <span class="navbar-toggler-icon"></span>
                <!-- Tampilkan profil user di samping burger icon -->
                <div class="d-block d-lg-none ms-2">
                    <img src="/foto/userPlaceholder.png" alt="User" class="rounded-circle"
                        style="width: 32px; height: 32px; object-fit: cover;">
                </div>
            </button>


            <!-- Isi Navbar -->
            <div class="collapse navbar-collapse" id="navbarCollapse">
                <!-- Menu Tengah -->
                <ul class="navbar-nav mx-auto mb-3 mb-lg-0 text-center">
                    <li class="nav-item"><a class="nav-link text-dark" href="#">Home</a></li>
                    <li class="nav-item"><a class="nav-link text-dark" href="#">Kategori</a></li>
                    <li class="nav-item"><a class="nav-link text-dark" href="#">Tentang Kami</a></li>
                </ul>

                <!-- Ikon dan Button -->
                <div class="d-flex flex-column flex-lg-row align-items-center gap-3 text-center">
                    <!-- ICON -->
                    <div class="d-flex flex-row flex-wrap justify-content-center gap-3">
                        <div class="d-flex flex-column align-items-center">
                            <a href="#" class="text-dark fs-6"><i class="bi bi-cart3"></i></a>
                            <small>Keranjang</small>
                        </div>
                        <div class="d-flex flex-column align-items-center">
                            <a href="#" class="text-dark fs-6"><i class="bi bi-opencollective"></i></a>
                            <small>Riwayat</small>
                        </div>
                        <div class="d-flex flex-column align-items-center">
                            <a href="#" class="text-dark fs-6"><i class="bi bi-clipboard-check-fill"></i></a>
                            <small>Progress</small>
                        </div>
                    </div>

                    <!-- Button -->
                    <!-- Jika user sudah login -->
                    <div class="user-profile d-none d-lg-block ms-3">
                        <img src="/foto/userPlaceholder.png" alt="User" class="rounded-circle"
                            style="width: 32px; height: 32px; object-fit: cover;">
                    </div>

                </div>
            </div>
        </div>
    </nav>

    <section style="padding-top: 50px;">
        <div class="container mt-5">
            <div class="card shadow p-4 mx-auto" style="max-width: 900px;">
                <div class="row">
                    <!-- Kiri: Info Produk -->
                    <div class="col-md-7">
                        <!-- Info Toko -->
                        <div class="d-flex align-items-center gap-2 mb-3">
                            <img src="/uploads/produk_img/hiya-hiya_20250514182837.jpg" class="rounded-circle"
                                width="40" height="40" style="object-fit: cover;" alt="Foto Toko">
                            <h5 class="mb-0 fw-semibold">Toko Serba Nyaman</h5>
                            <span class="badge bg-dark">Jakarta Barat</span>
                        </div>

                        <div class="d-flex align-items-start gap-4">
                            <!-- Foto Produk -->
                            <img src="/uploads/produk_img/hiya-hiya_20250514182837.jpg" class="img-thumbnail mb-3"
                                style="max-width: 220px;" alt="Foto Produk">

                            <!-- Teks di samping gambar -->
                            <div>
                                <h5 class="fw-bold">Nama Produk</h5>
                                <p class="mb-1">Provider: Telkomsel, XL</p>
                                <p>Harga: <strong>Rp 500.000</strong></p>
                            </div>
                        </div>


                        <!-- Form -->
                        <form action="/tampilan/data-pemesanan" method="POST">
                            <div class="mb-2">
                                <label class="form-label">Masking Sender:</label>
                                <input type="text" class="form-control" name="masking_sender">
                            </div>

                            <div class="mb-2">
                                <label class="form-label">Target Umur:</label>
                                <input type="text" class="form-control" name="target_umur">
                            </div>

                            <div class="mb-2">
                                <label class="form-label">Tipe Device yang Menerima:</label><br>
                                <div class="form-check form-check-inline">
                                    <input class="form-check-input" type="checkbox" name="tipe_device" value="Android" id="android">
                                    <label class="form-check-label" for="android">Android</label>
                                </div>
                                <div class="form-check form-check-inline">
                                    <input class="form-check-input" type="checkbox" name="tipe_device" value="Apple" id="apple">
                                    <label class="form-check-label" for="apple">Apple</label>
                                </div>
                                <div class="form-check form-check-inline">
                                    <input class="form-check-input" type="checkbox" name="tipe_device" value=">All Device" id="all">
                                    <label class="form-check-label" for="all">All Device</label>
                                </div>
                            </div>

                            <div class="row mb-2">
                                <div class="col">
                                    <label class="form-label">Tanggal Pengiriman:</label>
                                    <input type="date" class="form-control" name="tanggal_kirim" id="tanggal">
                                </div>
                                <div class="col">
                                    <label class="form-label">Jam Pengiriman:</label>
                                    <input type="time" class="form-control" name="jam_kirim">
                                </div>
                            </div>

                            <div class="mb-2">
                                <label class="form-label">Tipe Pengiriman:</label><br>
                                <div class="form-check form-check-inline">
                                    <input class="form-check-input" type="radio" name="tipe_kirim" id="sms" value="sms"
                                        checked>
                                    <label class="form-check-label" for="sms">SMS</label>
                                </div>
                                <div class="form-check form-check-inline">
                                    <input class="form-check-input" type="radio" name="tipe_kirim" id="mms" value="mms">
                                    <label class="form-check-label" for="mms">MMS</label>
                                </div>
                            </div>

                            <div class="mb-2" id="uploadMMS" style="display: none;">
                                <label class="form-label">Upload Gambar (untuk MMS):</label>
                                <input type="file" class="form-control" name="file_mms">
                            </div>

                            <div class="mb-2">
                                <label class="form-label">Kata-kata Pesan:</label>
                                <input type="text" class="form-control" name="kata_pesan">
                            </div>

                            <div class="mb-12">
                                <label class="form-label">Alamat Lokasi Target (ketik atau pilih dari peta):</label>
                                <input type="text" class="form-control" id="inputAlamat"
                                    placeholder="Contoh: Jl. Jend. Sudirman No.1, Jakarta">
                            </div>
                            <div class="mb-12">

                                <label class="form-label">Tandai Lokasi di Peta:</label>
                                <div id="map" style="text-align: center; height: 300px; width: 500px;"></div>
                                <input type="hidden" name="latitude" id="latitude">
                                <input type="hidden" name="longitude" id="longitude">
                                <input type="hidden" name="alamat_final" id="alamat_final"> <!-- alamat hasil drag -->
                            </div>

                            <div class="mb-3">
                                <label class="form-label">Catatan:</label>
                                <textarea class="form-control" rows="3" name="catatan"></textarea>
                            </div>

                            <button class="btn btn-primary">Ajukan Pemesanan</button>
                        </form>
                    </div>

                    <!-- Kanan: Jumlah -->
                    <div class="col-md-5 text-center">
                        <label class="form-label" >Jumlah Pemesanan:</label>
                        <div class="input-group justify-content-center"  style="max-width: 150px; margin: auto;">
                            <button class="btn btn-outline-secondary" type="button" onclick="ubahJumlah(-1)">-</button>
                            <input type="number" class="form-control text-center" name="jumlah" value="1" min="1" id="jumlah">
                            <button class="btn btn-outline-secondary" type="button" onclick="ubahJumlah(1)">+</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <footer class="bg-dark text-white mt-5 pt-4 pb-3">
        <div class="container">
            <div class="row text-center text-md-start">
                <!-- Brand dan Deskripsi -->
                <div class="col-md-4 mb-4">
                    <h5 class="fw-bold">NGIKLAN MURAH</h5>
                    <p class="small">Solusi mudah dan terjangkau untuk semua kebutuhan iklan Anda. Billboard, Radio, SMS
                        dan
                        lainnya.</p>
                </div>

                <!-- Navigasi -->
                <div class="col-md-4 mb-4">
                    <h6 class="fw-bold">Navigasi</h6>
                    <ul class="list-unstyled">
                        <li><a href="#" class="text-white text-decoration-none small">Home</a></li>
                        <li><a href="#" class="text-white text-decoration-none small">Fitur</a></li>
                        <li><a href="#" class="text-white text-decoration-none small">Harga</a></li>
                    </ul>
                </div>

                <!-- Kontak -->
                <div class="col-md-4 mb-4">
                    <h6 class="fw-bold">Kontak</h6>
                    <p class="small mb-1"><i class="bi bi-envelope me-2"></i>support@ngiklanmurah.com</p>
                    <p class="small"><i class="bi bi-telephone me-2"></i>+62 812-3456-7890</p>
                </div>
            </div>

            <!-- Copyright -->
            <div class="text-center border-top pt-3 small">
                © 2025 NgiklanMurah. All rights reserved.
            </div>
        </div>
    </footer>



    <script>
        //    efek scroll navbar
        window.addEventListener('scroll', function () {
            const navbar = document.querySelector('.navbar');
            navbar.classList.toggle('scrolled', window.scrollY > 50);
        });
        function ubahJumlah(delta) {
            const input = document.getElementById('jumlah');
            let val = parseInt(input.value);
            if (!isNaN(val)) {
                val += delta;
                if (val < 1) val = 1;
                input.value = val;
            }
        }

        const smsRadio = document.getElementById('sms');
        const mmsRadio = document.getElementById('mms');
        const uploadMMS = document.getElementById('uploadMMS');

        function toggleUpload() {
            uploadMMS.style.display = mmsRadio.checked ? 'block' : 'none';
        }

        smsRadio.addEventListener('change', toggleUpload);
        mmsRadio.addEventListener('change', toggleUpload);

        // jalankan saat halaman pertama kali dimuat
        toggleUpload();

        document.addEventListener("DOMContentLoaded", function () {
            const tanggalInput = document.getElementById("tanggal");
            if (tanggalInput) {
                const today = new Date();
                const year = today.getFullYear();
                const month = String(today.getMonth() + 1).padStart(2, '0');
                const day = String(today.getDate()).padStart(2, '0');
                const todayStr = `${year}-${month}-${day}`;
                tanggalInput.setAttribute("min", todayStr); // ⛔ Batas hanya bisa pilih dari hari ini ke atas
            }
        });


    </script>

    <script src="https://maps.googleapis.com/maps/api/js?key=<%= googleMapsApiKey %>&libraries=places&callback=initMap"
        async defer></script>

    <script>
        let map, marker, geocoder;

        function initMap() {
            const defaultLocation = { lat: -6.200000, lng: 106.816666 }; // Jakarta
            geocoder = new google.maps.Geocoder(); // ← Inisialisasi Geocoder

            map = new google.maps.Map(document.getElementById("map"), {
                center: defaultLocation,
                zoom: 12,
                mapTypeControl: false,
                streetViewControl: false,
                fullscreenControl: false
            });

            marker = new google.maps.Marker({
                position: defaultLocation,
                map: map,
                draggable: true
            });

            // Update lat/lng dan alamat saat marker digeser
            google.maps.event.addListener(marker, 'dragend', function () {
                const pos = marker.getPosition();
                updateLatLngInputs(pos.lat(), pos.lng());
                reverseGeocode(pos); // ← Dapat alamat berdasarkan posisi baru
            });

            const input = document.getElementById("inputAlamat");
            const autocomplete = new google.maps.places.Autocomplete(input);
            autocomplete.addListener("place_changed", function () {
                const place = autocomplete.getPlace();
                if (place.geometry) {
                    const location = place.geometry.location;
                    map.setCenter(location);
                    marker.setPosition(location);
                    map.setZoom(16);
                    updateLatLngInputs(location.lat(), location.lng());
                    document.getElementById("alamat_final").value = place.formatted_address || place.name;
                }
            });

            // Inisialisasi koordinat & alamat awal
            updateLatLngInputs(defaultLocation.lat, defaultLocation.lng);
            // reverseGeocode(defaultLocation);
        }

        function updateLatLngInputs(lat, lng) {
            document.getElementById("latitude").value = lat;
            document.getElementById("longitude").value = lng;
        }

        function reverseGeocode(latlng) {
            geocoder.geocode({ location: latlng }, function (results, status) {
                if (status === "OK") {
                    if (results[0]) {
                        document.getElementById("alamat_final").value = results[0].formatted_address;
                        document.getElementById("inputAlamat").value = results[0].formatted_address;
                    } else {
                        document.getElementById("alamat_final").value = "Alamat tidak ditemukan";
                    }
                } else {
                    console.error("Geocoder gagal karena: " + status);
                }
            });
        }

        window.initMap = initMap;
    </script>

</body>

</html>
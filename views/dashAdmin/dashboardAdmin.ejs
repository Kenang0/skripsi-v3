<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Dashboard</title>
  
  
</head>
<body>

  <!-- nav bar -->
   <div>
  <%- include('navbarDash.ejs') %>
</div>

  <div id="content">
    <%- include(partial, locals) %>

<!-- komentar # -->
    <%# include(partial, { error: error || {}, data: data || {} }) %> 

  </div>

</body>

